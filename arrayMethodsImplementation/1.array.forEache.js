const arr = ['Tigran', 'David', 'Aram', 'Mher', 'Arsen'];

for (let i = 0; i < arr.length; i++) {
    console.log(`Player:${arr[i]}`);
}

// const arr = ['Tigran', 'David', 'Aram', 'Mher', 'Arsen'];

// arr.forEach(item => {
//   console.log(`Player:: ${item}`);
// });

